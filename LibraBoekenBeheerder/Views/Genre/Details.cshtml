@model LibraBoekenBeheerder.Models.GenreModel

@{
    ViewData["Title"] = "Details";
}

<h1>Details</h1>

<div>
    <h4>Collections</h4>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.GenreId)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.GenreId)
        </dd>
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.GenreName)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.GenreName)
        </dd>
    </dl>
    <table>
        <tr>
            <th>Books</th>
        </tr>
        @foreach (BooksModel book in ViewBag.genreBooksList)
        {
            <tr>
                <td>@book.Title</td>
                <td>
                    <a asp-action="Details" asp-controller="Books" asp-route-id="@book.BookId" class="btn btn-success">Details</a>
                    <a asp-action="Edit" asp-controller="Books" asp-route-id="@book.BookId" class="btn btn-info" style="background-color:#FFE000; border-color:#FFE000">Edit</a>
                    <a asp-action="DeleteBook" asp-controller="Collections" asp-route-collectionID="@Model.GenreId" asp-route-bookID="@book.BookId" class="btn btn-danger" onclick="return confirm('Are you sure you want to remove this genre from this book?')">Delete</a>
                </td>
            </tr>
        }

    </table>
</div>
<div>
    <a asp-action="Index">Back to List</a>
</div>
